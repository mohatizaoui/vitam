# elasticsearch-mappings

Put here your custom elasticsearch-mappings in `.json` format.

To be associated with `metadata` or `metadata-collect`, you must edit the associated `mappingFile` in the `vitam_elasticsearch_tenant_indexation` variable.

> Please refer to the documentation for more details.
