---

- include_tasks: rsyslog.yml
  when: syslog.name | default('filebeat') == "rsyslog"

- include_tasks: syslogng.yml
  when: syslog.name | default('filebeat') == "syslog-ng"

- include_tasks: uninstall.yml
  when: syslog.name | default('filebeat') == "filebeat"
