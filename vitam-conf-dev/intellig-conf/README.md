# IntelliJ configuration

## Run/Debug Configurations

Copy the `runConfigurations` directory to `.idea` to automatically create all the required "Run/Debug Configurations".

You may need to restart IntelliJ to take it into account.
